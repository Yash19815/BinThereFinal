{
  "name": "binthere-dustbin-monitor",
  "version": "2.0.0",
  "description": "Smart dustbin monitoring web app using ESP32 ultrasonic sensors — real-time fill level dashboard.",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently --names \"server,client\" --prefix-colors \"green,blue\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "concurrently --names \"server,client\" --prefix-colors \"green,blue\" \"npm start --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Yash19815/ultrasonic-sensor-test-via-wifi.git"
  },
  "license": "MIT"
}
